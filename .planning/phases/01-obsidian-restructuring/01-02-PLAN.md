---
phase: 01-obsidian-restructuring
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .vault/产业趋势/
  - .vault/投资教训/
  - .vault/产业趋势/_index.md
  - .vault/投资教训/_index.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Industry trends are converted to individual Obsidian .md files"
    - "Investment lessons are converted to individual Obsidian .md files"
    - "Each trend has complete frontmatter with all JSON fields"
    - "Each lesson has complete frontmatter with all JSON fields"
    - "Both categories have _index.md summary files"
    - "Trends and lessons use tags and wikilinks for relations"
  artifacts:
    - path: ".vault/产业趋势/*.md"
      provides: "Individual trend notes"
    - path: ".vault/投资教训/*.md"
      provides: "Individual lesson notes"
    - path: ".vault/产业趋势/_index.md"
      provides: "Trends summary index"
    - path: ".vault/投资教训/_index.md"
      provides: "Lessons summary index"
  key_links:
    - from: "skills/memory/assets/memory/industry_trends/trends/*.json"
      to: ".vault/产业趋势/*.md"
      via: "JSON to Obsidian Markdown conversion"
    - from: "skills/memory/assets/memory/lessons_learned/lessons/*.json"
      to: ".vault/投资教训/*.md"
      via: "JSON to Obsidian Markdown conversion"
---

## Objective

Convert industry trends and investment lessons from JSON to Obsidian Markdown format.

**Purpose:** Complete the knowledge base content by converting all trend and lesson data to human-readable Obsidian notes.

**Output:**
- All industry trends converted to Obsidian .md files
- All investment lessons converted to Obsidian .md files
- _index.md files for both categories

## Context

@.planning/phases/01-obsidian-restructuring/01-CONTEXT.md

**Source Data:**
- Industry trends: `skills/memory/assets/memory/industry_trends/trends/*.json` (11 files)
- Investment lessons: `skills/memory/assets/memory/lessons_learned/lessons/*.json` (3 files)

**Decisions Implemented:**
- D-02: Complete frontmatter (all JSON fields preserved)
- D-03: Tags for categorization, wikilinks for relations
- D-05: Token optimization with collapsible callouts

## Tasks

<task type="auto" tdd="true">
<name>Task 1: Convert industry trends to Obsidian Markdown</name>
<files>.vault/产业趋势/*.md, .vault/产业趋势/_index.md</files>
<behavior>
- Each JSON file becomes one .md file with trend_id as filename
- Frontmatter contains: trend_id, trend_name, type, start_year, current_phase, tags, aliases, key_assets
- Content uses collapsible callouts for detailed sections
- Tags use #产业趋势 and #current_phase patterns
- Lifecycle stage clearly indicated
</behavior>
<action>
For each JSON file in `skills/memory/assets/memory/industry_trends/trends/`:

1. Read the JSON file
2. Create `.vault/产业趋势/{trend_id}.md` with:

```markdown
---
trend_id: "{trend_id}"
trend_name: "{trend_name}"
type: trend
start_year: {start_year}
current_phase: "{current_phase}"
tags:
  - 产业趋势
  - {current_phase}
aliases:
  - {trend_name}
keywords: {keywords array}
trigger_event: "{trigger_event}"
---

# {trend_name}

> [!info] 趋势概况
> - **起始年份**: {start_year}
> - **当前阶段**: {current_phase}
> - **触发事件**: {trigger_event}
> - **关键词**: {keywords joined}

## 趋势详情

> [!details]- 详细描述
> {trend_details}

## 市场影响

{For each market section:}

### {market_name}
{market content}

## 投资机会

### 短期
{short_term items or "暂无"}

### 中期
{medium_term items or "暂无"}

### 长期
{long_term items or "暂无"}

## 教训

{lessons as list}

## 当前状态

> [!tip] 状态监控
> - **阶段**: {phase}
> - **关注指标**: {key_watch joined}
> - **风险因素**: {risk_factors joined}
```

3. Create `.vault/产业趋势/_index.md`:
```markdown
---
title: 产业趋势索引
type: index
tags:
  - 索引
---

# 产业趋势知识库

收录产业趋势及其生命周期分析。

| 趋势ID | 名称 | 起始年 | 当前阶段 | 触发事件 |
|---------|------|--------|----------|----------|
{table rows for each trend}
```
</action>
<verify>
<automated>ls .vault/产业趋势/*.md 2>/dev/null | wc -l | xargs test $(ls skills/memory/assets/memory/industry_trends/trends/*.json 2>/dev/null | wc -l) -eq && echo "PASS" || echo "FAIL"</automated>
</verify>
<done>All trend JSON files converted to Obsidian Markdown with complete frontmatter</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Convert investment lessons to Obsidian Markdown</name>
<files>.vault/投资教训/*.md, .vault/投资教训/_index.md</files>
<behavior>
- Each JSON file becomes one .md file with lesson_id as filename
- Frontmatter contains: lesson_id, type, related_crisis, asset, lesson_date, tags, aliases
- Content includes judgment, actual result, error analysis, and lessons learned
- Tags use #投资教训 and #asset patterns
- Related crisis referenced via wikilinks
</behavior>
<action>
For each JSON file in `skills/memory/assets/memory/lessons_learned/lessons/`:

1. Read the JSON file
2. Create `.vault/投资教训/{lesson_id}.md` with:

```markdown
---
lesson_id: "{lesson_id}"
type: lesson
related_crisis: "[[{related_crisis}]]"
asset: "{asset}"
lesson_date: "{date}"
tags:
  - 投资教训
  - {asset}
aliases:
  - "{date} {asset}教训"
judgment_direction: "{direction}"
confidence: {confidence}
actual_change: "{actual_change}"
---

# {date} {asset}投资教训

## 判断

- **资产**: {asset}
- **方向**: {direction}
- **置信度**: {confidence}

## 实际结果

- **实际变动**: {actual_change}

## 错误分析

> [!danger] 根本原因
> {root_cause}

## 教训总结

{lessons_learned as list}

## 规避策略

> [!tip] 未来建议
> {avoidance_strategy}

## 相关危机

[[_ {related_crisis}_]]
```

3. Create `.vault/投资教训/_index.md`:
```markdown
---
title: 投资教训索引
type: index
tags:
  - 索引
---

# 投资教训知识库

收录投资错误判断及其教训。

| 教训ID | 日期 | 资产 | 方向 | 实际结果 | 根本原因 |
|--------|------|------|------|----------|----------|
{table rows for each lesson}
```
</action>
<verify>
<automated>ls .vault/投资教训/*.md 2>/dev/null | wc -l | xargs test $(ls skills/memory/assets/memory/lessons_learned/lessons/*.json 2>/dev/null | wc -l) -eq && echo "PASS" || echo "FAIL"</automated>
</verify>
<done>All lesson JSON files converted to Obsidian Markdown with complete frontmatter</done>
</task>

## Verification

- [ ] All industry trend JSON files have corresponding .md files
- [ ] All investment lesson JSON files have corresponding .md files
- [ ] Each .md file has valid frontmatter
- [ ] `_index.md` files created for both categories
- [ ] Tags and wikilinks properly formatted
- [ ] Related crises linked via wikilinks

## Success Criteria

- All trends converted with lifecycle stage and market impact (D-02, D-03)
- All lessons converted with error analysis and avoidance strategies (D-02)
- Both categories use collapsible callouts for token optimization (D-05)
- Cross-references established between lessons and related crises (D-03)

## Output

After completion, create `.planning/phases/01-obsidian-restructuring/01-02-SUMMARY.md`